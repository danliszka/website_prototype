window.addEventListener('load', function(){
    var btns = document.getElementsByClassName('btn-secondary');
    for (i = 0; i < btns.length; i++)
        {
            btns[i].addEventListener('click', openTab);
        }
});

function openTab(ev){
    
}